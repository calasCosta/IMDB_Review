"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
function nGram(from, to) {
    if (typeof from !== "number" || isNaN(from) || from < 1 || from === Infinity) {
        throw new Error("`" + from + "` is not a valid argument for n-gram");
    }
    to = to || from;
    if (to < from) {
        throw new Error("to must be larger tha from");
    }
    return grams;
    function grams(value) {
        const nGrams = new Set();
        if (value === null || value === undefined) {
            return Array.from(nGrams);
        }
        value = value.slice ? value : String(value);
        for (let i = from; i <= to; i++) {
            let index = value.length - i + 1;
            if (index < 1) {
                continue;
            }
            while (index--) {
                nGrams.add(value.slice(index, index + i));
            }
        }
        return Array.from(nGrams);
    }
}
exports.default = nGram;
exports.bigram = nGram(2);
exports.trigram = nGram(3);
//# sourceMappingURL=index.js.map